# 룬테라 전쟁 — 멀티(공유월드) 설계안 v1 (확정 대기)

> 목적: 팀원끼리 **하나의 공유 월드**에서 서로의 실제 마을을 보고 **공격/지원/정복**. 라이브 공유 경제·오픈 DB 규칙을 감안한 안전 설계.

## 0. 지금 구조(그대로 활용 가능한 토대)
- `genWorld(778899)` = **고정 시드** → 모든 플레이어가 **같은 야만인 월드·같은 좌표계**를 봄. **집(HOME)만 무작위**. (이미 "멀티 대비"로 설계돼 있음)
- 라이벌 AI 마을·습격은 이미 제거됨 → 그 자리에 **실제 팀원 마을**을 얹으면 됨.
- 렌더/전투 자산 이미 존재: `owner:'player'` 렌더(rally_3 타워)·`marches`(attack/support/scout/return)·`resolveAttack`·정복(충성도 0)·`spy`·`reports`·mail 없음(신설 필요).
- 신원 = 사이트 골드키(닉네임). 저장 = `runewar/players/<키>`(이미 구현). Firebase 규칙 = **공개(read/write 열림)**.

## 1. 월드 모델
- **단일 공유 월드** `worldId='w1'`(시즌 붙으면 `s2-w1` 식). 좌표계는 genWorld 그대로.
- 각 플레이어의 **집 위치 등록**: 시작(startGame) 시 무작위 좌표가 정해지면 공유 레지스트리에 등록. **충돌 검사**(이미 점유된 칸이면 재추첨) 필요 → 등록은 트랜잭션.

## 2. Firebase 스키마 (신규)
```
runewar/
  players/<키>            # (기존) 내 전체 세이브 — 본인만 씀
  log/<키>/<pushId>       # (기존) 감사 로그
  world/w1/
    villages/<coordKey>   # coordKey = "x,y". 공유 레지스트리(모두 읽음, 소유자만 씀)
        = { owner:<키>, name, x, y, size, pts, wall,
            def:<수비력 스냅샷>, garrisonHint, ts:updatedAt }
    claims/<coordKey>     # 집/정복 좌표 선점(트랜잭션) = owner 키
  mail/<키>/<pushId>      # 전투 결과·피습 알림(상대가 나에게 씀) — 비대칭 쓰기
  presence/<키>          # {online, lastSeen} — onDisconnect로 오프라인 표시
```
- **쓰기 원칙**: 각자 **자기 마을 노드만** 갱신(`world/w1/villages/<내좌표>`). 남의 노드엔 **전투 결과 mail만** 씀(비대칭). 오픈 규칙이라 기술적으론 아무나 쓰지만, **신뢰된 팀원 베타** 전제 + 서버 검증은 후순위(§7).

## 3. 가시성(다른 플레이어 마을 보기)
- 맵 진입 시 `world/w1/villages` 구독(onValue). 내 좌표 외의 엔트리를 `owner:'player'`로 렌더(이미 지원).
- **전장의 안개** 유지: 공개된 헥스만 상대 마을 표시(정찰/행군으로 밝힘). 위치 노출 최소화.
- 라벨: 상대 닉네임 + 발전점수(이미 `_mkMarkHtml` 지원).

## 4. 전투(비동기 · 부족전쟁식)
- **행군 전송**: 공격/지원 부대를 상대 좌표로 send(travel time 후 도착). 내 `marches`에 기록 + (선택)상대 mail에 "인바운드" 예고.
- **도착 해소 = 공격자 클라가 처리**(서버 없음):
  1) 공격자 클라가 도착 시각에 상대 `villages/<좌표>.def`(마지막 스냅샷) 읽어 `resolveAttack` 실행.
  2) 결과 → **공격자 세이브**(약탈물·귀환군) + **상대 mail**(피습 리포트) + **상대 마을 노드**(수비 감소·성벽·충성도) 갱신.
  3) 공격자 클라가 닫혀 있으면 → **다음 접속 때 밀린 행군 일괄 처리**(arriveAt 지난 것). 이미 `processMarches`가 시간기반이라 확장 용이.
- **정복**: 충성도 0 → 상대 `villages/<좌표>.owner`를 내 키로, claims 트랜잭션으로 이전. 상대 세이브에서 그 마을 제거는 상대 클라가 mail 보고 반영(또는 owner 불일치 감지 시 자동 정리).
- **지원**: 상대 마을에 주둔군 추가(`villages/<좌표>.support[내키]`). 회수는 집결지.

## 5. 동시성·충돌
- **집/정복 좌표 선점** = `runewar/world/w1/claims/<좌표>` **runTransaction**(비어있을 때만 차지). 동시 정복/동시 시작 방지.
- **마을 노드 갱신** = last-write-wins + `ts`. 수비 스냅샷은 근사치라 약간의 경합 허용(베타).
- **멀티기기**: 기존 `_ctrlGuard`로 한 계정 다기기 동시 조작 잠금(이미 있음).

## 6. 마이그레이션(기존 로컬 플레이어 → 공유월드)
- 기존 로컬 세이브(자기 월드)에서 **내 마을들만** 공유 레지스트리에 등록(좌표 충돌 시 재배치 1회).
- 야만인 월드는 고정 시드라 그대로 일치 → 추가 작업 없음.
- SAVE_VER +1 + `migrate()`로 무손실 승급(이미 구현).

## 7. 안티치트(오픈 규칙 대응) — 베타 후 단계적
- 지금(신뢰 팀원 베타): 클라 신뢰. 단 **감사 로그**로 모든 정복/전투 기록 → 사후 검토·롤백 가능.
- 다음: Firebase **보안 규칙**으로 `world/w1/villages/<좌표>`는 `owner==auth.uid`만 쓰기 허용(Auth 도입 필요) + Cloud Function으로 전투 서버검증. ⚠️ 현재 규칙 완전 공개라 **정식 오픈 전 필수**.

## 8. 단계(Phase) 제안
- **P1 가시성**: 공유 레지스트리 등록/구독 → 팀원 마을이 서로 보임(전투 없음). 리스크 낮음.
- **P2 지원**: 우군 마을에 주둔 지원 + 회수. (비파괴)
- **P3 공격/약탈**: 비동기 공격·리포트·약탈물. 밀린 행군 처리.
- **P4 정복**: 충성도·claims 트랜잭션·소유 이전.
- **P5 안티치트**: Auth + 보안 규칙 + (선택)Function 검증. **정식 오픈 게이트**.

## 9. 결정 필요 항목(확정 요청)
1. 월드 1개 공유가 맞는지(팀 규모 — 몇 명 예상? 한 월드에 다 수용?).
2. 공격 대상: **팀원 서로 공격 허용** vs 지원만(협동)? (부족전쟁은 서로 공격, 협동형이면 지원/공동 야만인 토벌만)
3. 약탈로 뺏는 자원의 성격(게임 자원만 — 사이트 골드 무관 확정).
4. P1(가시성)부터 바로 착수 OK?

---
*이 문서 = 확정 대기. 합의되면 Phase 순서대로 구현·배포. 상세 진행은 리포 CLAUDE.md 세션이력에 반영.*
